{
  "name": "milk-farm-management-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./milk-farm-management-master run dev",
    "build": "npm --prefix ./milk-farm-management-master run build",
    "preview": "npm --prefix ./milk-farm-management-master run preview",
    "dev:web": "npm --prefix ./milk-farm-management-master run dev",
    "build:web": "npm --prefix ./milk-farm-management-master run build",
    "preview:web": "npm --prefix ./milk-farm-management-master run preview",
    "api": "node ./backend/server.js",
    "deploy:web": "npm run build:web && npx firebase-tools deploy --only hosting",
    "deploy:api": "gcloud run deploy milk-farm-api --source ./backend --region us-central1 --allow-unauthenticated --set-env-vars API_PORT=8080,API_HOST=0.0.0.0"
  }
}
